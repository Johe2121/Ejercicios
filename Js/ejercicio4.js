//Funciones
function registerName() {
    // cuando una variable está dentro de la función es local y no existe fuera de esta...
    let userName = prompt("Ingrese su nombre");
    alert("Tu nombre de usuario ingresado es: " + userName);
}